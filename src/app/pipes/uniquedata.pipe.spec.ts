import { UniquedataPipe } from './uniquedata.pipe';

describe('UniquedataPipe', () => {
  it('create an instance', () => {
    const pipe = new UniquedataPipe();
    expect(pipe).toBeTruthy();
  });
});
